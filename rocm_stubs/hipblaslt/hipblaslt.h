#pragma once
// Stub for paroquant NixOS build — paroquant kernel does not use hipblaslt
typedef void* hipblasLtHandle_t;
typedef void* hipblasLtMatmulDesc_t;
typedef void* hipblasLtMatrixLayout_t;
typedef void* hipblasLtMatmulPreference_t;
typedef int hipblasLtComputeType_t;
typedef int hipblasDatatype_t;
